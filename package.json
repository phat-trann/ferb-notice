{
  "name": "ferbnotice-extension",
  "version": "1.0.0",
  "private": true,
  "description": "FerbNotice Chrome extension. Made by Ferb.",
  "engines": {
    "node": ">=25.9.0"
  },
  "scripts": {
    "build": "tsc && mkdir -p dist && cp -R static/. dist",
    "build:production": "npm run clean && npm run typecheck && npm run build",
    "clean": "rm -rf dist ferbnotice-production.zip",
    "pack": "npm run build:production && cd dist && zip -r ../ferbnotice-production.zip .",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.29",
    "typescript": "^5.9.3"
  }
}
